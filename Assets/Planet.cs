using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Planet : MonoBehaviour
{
    // public System.Action<Transform, Transform> OnCollidedWith;


    // FixedJoint fixedJointObj;
    // void OnCollisionStay(Collision collisionInfo)
    // {
    //     if(fixedJointObj == null )
    //         fixedJointObj = gameObject.AddComponent<FixedJoint>();

    //     Rigidbody other;

    //     if ((other = collisionInfo.gameObject.GetComponent<Rigidbody>()) is null)
    //         return;

    //     fixedJointObj.connectedBody = other;
    // }
}
